<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= name %></title>
  </head>
  <body>
    <%- include('./partials/nav') %>
    <h1><%= name %></h1>

    <p>Address: <%= address %></p>
    <p>Phone: <%= phone %></p>

    <%if (open) {%>
    <p>Yes! We're Open.</p>
    <% } else { %>
    <p>Sorry! We're closed right now.</p>
    <% } %>
  </body>
</html>
